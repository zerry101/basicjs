"use strict";
let sales = 123456789; //autmatically associates datatype when not mentioned hover mouse on variable for info
let course = 'typescript';
let is_published = true;
function render(document) {
    console.log(document);
}
